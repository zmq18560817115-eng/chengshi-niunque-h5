# 2026-08-18 前后台联调与部署走查

## 结论

应用层联调通过：生产构建可启动，健康接口正常，公开内容接口返回 3 个正式分类，后台已登录会话可读取相同数据库并显示 3 个正式分类。代码质量门禁全部通过。

本次不能确认容器级部署闭环，因为执行环境未安装 Docker；生产编排也尚未包含 PostgreSQL、对象存储、迁移任务和备份恢复。

## 流程步骤

1. 前台入口 `/go`：健康。375 基准构图无页面滚动，入口按钮可进入档案。
2. 公开档案 `/reports`：健康。展示检测项目、复核保障、生产溯源三个分类，数据来自 `/api/public/content`。
3. 后台 `/admin`：健康。现有测试管理员会话有效，工作台可读取分类与内容统计。
4. 后台 `/admin/modules`：功能健康、移动端体验有风险。三类正式分类及卡片/资料统计可读取；窄屏导航纵向占用较高，但后台并非 H5 主交付面。
5. 接口联调：健康。`/api/health` 返回 `status: ok`；`/api/public/content` 返回 3 个公开分类。
6. 构建与自动化：健康。lint、typecheck、Prisma schema、生产 build 全部通过；Vitest 19 个文件、78 项测试全部通过。
7. 容器部署：未验证。当前机器没有 Docker 命令，未执行镜像构建、Compose 启动和 Nginx 反向代理实测。

## 证据

- `01-home-375x812.png`：前台品牌入口。
- `02-archive-375x812.png`：公开档案入口。
- `03-admin-login-375x812.png`：后台工作台（文件名沿用首轮捕获，实际为已登录状态）。
- `04-admin-modules-375x812.png`：后台分类管理。

浏览器截图服务在本轮对显式视口存在像素倍增/平铺现象；DOM 尺寸与滚动指标另行核对。截图可证明页面状态和内容，但不能单独作为逐像素视觉验收结论。

## 风险与建议

- 高：生产迁移没有编排入口。发布流程必须新增独立 `prisma migrate deploy` 任务。
- 高：生产数据库和对象存储不在 `compose.production.yaml` 内，部署方必须明确提供并备份。
- 中：`/api/health` 只证明应用存活，不能证明数据库和对象存储可用。
- 中：后台在窄屏下导航占用较多，建议后台主要按桌面使用；若要求手机后台，需要单独做响应式验收。
- 低：仓库根目录存在两个运行时无关的异常已跟踪文本文件，建议确认后清理。
- 已处理：新增 `.dockerignore`，真实 `.env`、依赖、构建缓存、测试产物和原始输入不会进入 Docker 构建上下文。

