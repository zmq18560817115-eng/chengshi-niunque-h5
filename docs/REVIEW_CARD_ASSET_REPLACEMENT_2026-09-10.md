# 复核保障模块素材替换（2026-09-10）

按用户提供的 `3-报告检测页面-模块2/报告页2完整图.jpg` 和 `报告点击页2-部件/` 替换报告检测分类页的第三张卡片。首页长图不在本次修改范围内。

## 变更

- 标题：产品基础型检 → 过敏原项筛查；源部件 `资源 111.png` → `资源 147.png`。
- 说明：工厂出厂检测和第三方检测，双层兜底检测 → 提前为宝宝排查150项致敏风险；源部件 `资源 128.png` → `资源 146.png`。
- 素材库完整参考图替换为 `报告页2完整图.jpg`，源文件逐字节保留。其余 22 个部件与新素材包的 SHA-256 完全一致，无需重编码。
- 原图中标题源部件定位为 `(254, 2873)`，说明源部件定位为 `(254, 2998)`。裁掉透明边缘后的运行时位置分别为 `(258, 2876)`、`(256, 3000)`，坐标基于 2000 × 4333 原图。
- 新运行时文件为 `review-147.webp`、`review-146.webp`，并更新素材清单与页面引用。移除已替代的两个旧部件及旧参考图。

卡片 ID `seed-card-review-stability-sensory`、报告绑定和跳转地址保持不变。已有默认卡片的旧文案会显示为本次批准文案；后台自定义标题和说明仍保留。其他卡片文案、布局、按钮、动画、首页及第四层报告页样式均未更改。

## 重新生成

`python scripts/refresh-design-assets.py --review-card-copy`

此参数只生成复核保障第三张卡片的标题、说明。全量构建配置也已同步新部件编号，说明保留原图配准位置。

## 验证

- `pnpm lint`、`pnpm typecheck`、`pnpm prisma:validate`、`pnpm build` 通过。
- `pnpm test`：303 通过，11 失败，6 跳过。失败涉及本机 `localhost:5432` PostgreSQL 不可用及依赖它的健康检查；本次文案、默认配置、分类主题相关测试均通过。
- 使用实际 `CategoryDetail` 组件及项目 CSS，在 375 × 812、375 × 667、390 × 844 的 Chrome 手机视口验证。按钮点击区域与替换前完全一致，跳转仍使用原卡片 ID，旧默认文案兼容及后台自定义文案显示正常。
- 浏览器验证采用固定内容数据和 Next 图片/路由适配器；未以本机数据库验证管理后台完整链路。
- 素材清单全部源文件校验通过，生成配置除第三张卡片的标题、说明及其两份图片信息外完全一致。

本地截图和检查结果保存在忽略目录 `artifacts/review-page-refresh-2026-09-10/`。

## 后台标题同步补充

后台项目名称已复用前端卡片文案；本次补齐已上传报告的旧标题映射。固定项目下原先自动命名的“产品基础型检报告”或“稳定性与感官报告”统一显示为“过敏原项筛查报告”，后台报告标题、缩略图说明与前端报告查看器使用同一解析规则。

解析按分类及固定卡片 ID 匹配，仅替换该卡片的精确旧默认名称。自定义报告名称、其他项目中的同名资料、报告 ID、图片页、文件、发布状态及上传版本保持不变。更新在查询输出时生效，不需要重新上传报告或重写数据库记录。

新增回归测试通过实际管理服务和管理组件验证后台标题、图片说明、跳转地址，并对照公开服务输出。相关 4 个测试文件共 23 项通过。

补充修改后 `pnpm lint`、`pnpm typecheck`、`pnpm prisma:validate`、`pnpm build` 均通过。全量测试为 305 通过、11 失败、6 跳过，失败仍为本机 PostgreSQL 不可用及关联健康检查；日志位于 `artifacts/admin-title-sync-2026-09-10/`。
