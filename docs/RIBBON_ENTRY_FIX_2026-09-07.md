# 首页紫色提示条重复入场修复

问题来自两处同时展示：`archive-1-book-stage.webp` 完整书本中已经带有紫色资源 4，独立动画层又展示一次资源 4。原有验证只数独立图片元素，无法发现底图内的重复。

当前用 `ArchiveRibbonBacking` 在紫色条原位置显示同张书本的相邻黄色边缘肌理与原始资源 22 金色飘带，遮住底图内的静态副本。首页、引导页过渡、首页加载预览使用同一组坐标和图层，原素材文件没有更改。背景源的不确定性记录在 `OPEN_QUESTIONS.md` 第 27 项。

独立资源 4 初始为不可见且透明，待图片、页面交接完成且所在位置进入视口后，从右向左渐显 800ms。动画只播放一次，完成后恢复静态文档坐标，随页面滚动。减少动态效果模式和加载失败时继续保留静态可读回退。

验收脚本 `scripts/verify-ribbon-entry.mjs` 增加截图像素检测：尚未入场时不得残留紫色像素，入场后必须出现紫色资源；同时检查渐显过程、单次播放、文档坐标、横向无溢出、引导过渡与慢加载预览。覆盖 Chromium、WebKit，375/430 竖屏及 844 横屏，并检查减少动态效果模式。截图与结果保存在忽略目录 `artifacts/ribbon-entry-qa/`。

通过 lint、typecheck、304 项完整测试、Prisma 校验和生产构建；最终纹理取样调整后另通过 67 项相关单元测试和生产构建。
