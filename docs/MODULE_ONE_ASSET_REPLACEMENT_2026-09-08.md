# 首页模块一素材替换

用户来源：`C:/Users/bu/Desktop/2-长图模块1-1/`。本次按用户明确要求更新首页长图模块一及系统设计素材库，保留现有交互、动画和其他模块。

## 替换范围

- 素材库目录继续使用 `public/design/2026-09-07/source/2-长图模块1/`，全部 23 个文件与本次提供的原始文件 SHA-256 一致。
- 旧 `长图模块一完整.png` 替换为 `长图模块1完整图.jpg`；旧 `资源 22.png` 替换为 `资源 24.png`，页面共用的飘带遮挡图层同步指向资源 24。已移除两份被替代的旧文件。
- 重建 `archive-1-book-stage.webp`、`archive-1-batch-module.webp`、`archive-batch-editable.webp` 及普通／可编辑整页预览的第一模块。相关 URL 加入 `?v=20260908`，避免旧缓存。
- 新完整图为 2000×3698，原模块占位为 2000×3733。使用同包公共底纹补齐底部 35px；画布整体仍为 2000×10783，模块二、三起点仍为 3733、7974。
- 书本与整块批次卡片的几何、分组不变。独立紫色提示条按新原稿配准向右调整 3 个原图像素（375px 屏宽下约 0.56px），其入场时长、触发与滚动行为不变。
- 普通、后台预览、自定义批次与引导过渡共享新素材。普通与可编辑整页预览沿用 WebP 压缩，分别约 788KB、776KB；后两模块仅发生整张预览重新编码，独立运行时素材及坐标完全不变。
- 本次未改动 `docs/input/`、服务端数据、上传报告、报告页样式或任何动画控制代码。前一轮第四层报告页顶部压缩保留。

## 构建与验证

`scripts/refresh-design-assets.py --archive-module-one` 可仅重建模块一及对应预览；需要 Pillow 与 NumPy。全量素材配准、构建仍使用 OpenCV。生成器保留新版来源、35px 底纹处理和缓存版本，现有资源存在性测试改为正确识别带版本参数的 URL。

证据位于忽略目录 `artifacts/module-one-refresh/`：

- `scope-verification.json`：比对替换前后的素材与源码哈希。配置变化仅涉及模块一及整页预览；模块二、三独立图片和动画源码未变。
- `browser-results.json`：真实 `ReportsArchive` 组件配合本地样例数据和路由适配器，完成 16 组检查：5 档屏幕前后预览、首页直入、引导入场、自定义批次、过渡图层新资源 URL、三个分类入口。无破图、横向溢出。
- `unchanged-lower-modules.json`：下方模块前后截图对比。320px、750px 完全一致，其他尺寸平均通道差不超过 0.031/255，极少量边缘采样像素有差异；所有独立素材源文件保持原哈希。
- `module-one-final-375.png` 与 `live-*-375.png`：最终首屏及各入口截图。
- `relevant-tests.log`：交互、动画隔离、批次显示相关 105 项测试全部通过。

`pnpm lint`、`pnpm typecheck`、`pnpm prisma:validate`、`pnpm build` 通过。完整 `pnpm test` 结果为 302 项通过、11 项失败、6 项跳过；11 项失败均依赖未启动的本地 PostgreSQL，真实数据库／对象存储联调未在本轮完成。本轮未执行部署。
